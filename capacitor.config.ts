import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bhavan.finance',
  appName: 'Personal Finance Tracker',
  webDir: 'dist'
};

export default config;
